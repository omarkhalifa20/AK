import { CartMod } from '@/Types/Cartmod'
import React from 'react'

export default function CartCard({item} : {item:CartMod[]}) {
  return (
    <div className='flex flex-col bg-white p-2 rounded-lg '>
    
    </div>
  )
}
